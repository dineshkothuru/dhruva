{
  "bug-fix": {
    "id": "bug-fix",
    "title": "Bug fix",
    "description": "Locate the root cause, gate on the fix plan, implement, review the diff, validate and deploy the changed files.",
    "inputs": [
      {
        "key": "description",
        "label": "Bug description",
        "kind": "text",
        "attachTo": true
      },
      {
        "key": "runTests",
        "label": "Run local Apex tests during validation",
        "kind": "boolean",
        "default": false
      },
      {
        "key": "deploy",
        "label": "Deploy to the default org at the end",
        "kind": "boolean",
        "default": true
      },
      {
        "key": "visualTest",
        "label": "Visual test before deploy (opens the org in the browser with your LOCAL UI files + live org data)",
        "kind": "boolean",
        "default": false
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot baseline",
        "type": "snapshot"
      },
      {
        "id": "locate",
        "title": "Locate root cause (agent, read-only)",
        "type": "agent",
        "prompt": "You are investigating a bug. DO NOT modify any files in this step.\nBug report: {inputs.description}\nInvestigate the local codebase and report the diagnosis in EXACTLY this structure:\nSYMPTOMS: <what is broken - error messages, logs, observed behavior from the report>\nREPRODUCTION: <numbered steps that trigger it, ending in the observed failure; 'not reproducible locally' plus why, if so>\nEXPECTED vs ACTUAL: <one line each>\nEVIDENCE: <what you checked - classes read, automation traced, config examined>\nDATA FLOW: <trace from entry point to the failure point; where it diverges from expected>\nROOT CAUSE: <the actual cause, not a symptom or a guess - with the exact file and line/element reference. If you cannot establish it with evidence, say UNCONFIRMED and list the candidates>\nIMPACT: <severity critical/high/medium/low + which users/features are affected + since when if determinable, else 'unknown'>\nFIX PLAN: <the concrete fix for exactly this root cause, files to change, and the test that will prove it>\nUI BUGS (component not rendering, field missing, wrong layout, broken interaction): the sections still apply, read them as follows. DATA FLOW = the render/event path - flexipage/layout -> component -> wire/controller -> data -> conditional rendering, or click -> handler -> apex -> response. EVIDENCE = markup AND configuration: the component HTML/JS, field-level security, page layouts/flexipages and visibility filters, record types, validation rules, permission sets - many UI symptoms are config visibility, not code. You cannot see a browser: reason from the reported description/screenshots, mark REPRODUCTION as 'visual - verify via the Local Dev preview' where applicable, and say so when the root cause needs on-screen confirmation.\nEnd your reply with one line listing every involved file as project-relative paths:\nFILES: force-app/main/default/classes/Example.cls, force-app/main/default/classes/ExampleTest.cls",
        "readOnly": true,
        "role": "read",
        "emits": "work"
      },
      {
        "id": "approve-plan",
        "title": "Approve fix plan",
        "type": "gate",
        "message": "Review the root-cause analysis and fix plan above. Proceed with implementing it?"
      },
      {
        "id": "work-check",
        "title": "Is the bug still there to fix?",
        "type": "work-check"
      },
      {
        "id": "retrieve-fresh",
        "title": "Retrieve fresh copies of affected files from the org",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "retrieve",
          "start",
          "{affectedSourceDirs}",
          "--json",
          "--wait",
          "10"
        ],
        "optional": true
      },
      {
        "id": "retrieve-delta",
        "title": "Compare retrieved files against the investigated versions",
        "type": "changes"
      },
      {
        "id": "rebaseline",
        "title": "Re-baseline after org refresh",
        "type": "snapshot"
      },
      {
        "id": "implement",
        "title": "Implement the fix (re-verifies only if the org drifted)",
        "type": "agent",
        "prompt": "Implement the fix for this bug.\nBug report: {inputs.description}\nApproved plan from the investigation step:\n{steps.locate.output}\n\nThe affected files were re-retrieved from the org after the investigation. This is the exact delta between what the investigation saw and the org's current version:\n{steps.retrieve-delta.output}\nUse that delta ONLY to decide whether the investigation is still valid. Ignore any path outside force-app - harness bookkeeping under .dhruva is not org drift.\n\n- No force-app file listed: the investigation saw exactly what the org has. Implement the plan directly. Do NOT re-investigate.\n- A force-app file listed that the investigation named: re-read THAT file, confirm the bug is still present and the plan still applies, then implement. If the fresh code already contains the fix, say so and change nothing.\n- A force-app file listed that the investigation did not name: note it in your output and carry on. It is not evidence the diagnosis is wrong.\n\nYou are not re-doing the investigation. The root cause was found, reviewed and approved; your job is to apply it to the current code, and to notice only if the current code has moved out from under it.\n\nNever create a new file, class, or method when an existing one should be edited - search for existing implementations first and modify them. Only change what the plan requires. Do not deploy.",
        "role": "implement"
      },
      {
        "id": "changes",
        "title": "Collect changed files",
        "type": "changes"
      },
      {
        "id": "verify-standards",
        "title": "Verify standards on changed files (deterministic)",
        "type": "verify"
      },
      {
        "id": "review",
        "title": "Code review of the changes (agent, read-only)",
        "type": "agent",
        "prompt": "Review ONLY the changes listed below (made in this run) against the team standards. Do not modify any files.\nChanged files:\n{steps.changes.output}\nDeterministic standards-check result:\n{steps.verify-standards.output}\nRead each changed file and review the actual change.",
        "readOnly": true,
        "persona": "salesforce-review",
        "role": "review",
        "autoRevise": {
          "target": "implement",
          "trigger": "VERDICT:\\s*BLOCKED",
          "maxRounds": 3
        },
        "emits": "findings"
      },
      {
        "id": "approve-changes",
        "title": "Approve code changes",
        "type": "gate",
        "message": "HUMAN GATE - you are shown the diff and the code reviewer's verdict with its findings. Revise to rework, approve to validate and deploy the fix.",
        "reviseTarget": "implement"
      },
      {
        "id": "validate",
        "title": "Validate (check-only deploy of changed files)",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "{changedSourceDirs}",
          "--json",
          "--wait",
          "30"
        ]
      },
      {
        "id": "validate-tests",
        "title": "Validate with local tests",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "{changedSourceDirs}",
          "--test-level",
          "RunLocalTests",
          "--json",
          "--wait",
          "60"
        ],
        "onlyIf": "runTests"
      },
      {
        "id": "visual-preview",
        "title": "Visual test - local files against live org data (Local Dev)",
        "type": "cli",
        "bin": "sf",
        "args": [
          "lightning",
          "dev",
          "app"
        ],
        "optional": true,
        "detached": true,
        "onlyIf": "visualTest"
      },
      {
        "id": "approve-deploy",
        "title": "Approve deploy",
        "type": "gate",
        "message": "Validation passed. If you enabled the visual test, click through the app in the browser first. Deploy the changed files to the default org now?",
        "onlyIf": "deploy"
      },
      {
        "id": "deploy",
        "title": "Deploy changed files",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "{changedSourceDirs}",
          "--json",
          "--wait",
          "30"
        ],
        "onlyIf": "deploy"
      }
    ]
  },
  "deploy-preview": {
    "id": "deploy-preview",
    "title": "Deploy preview",
    "description": "Read-only: list exactly what a deploy from this folder would change in the org. Requires a SOURCE-TRACKED org (scratch org, or a Developer/Dev Pro sandbox with tracking enabled) - regular sandboxes fail with NonSourceTrackedOrgError; use Validate deploy there instead.",
    "inputs": [],
    "steps": [
      {
        "id": "preview",
        "title": "Preview deploy against default org",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "preview",
          "--json"
        ]
      }
    ]
  },
  "design-to-docs": {
    "id": "design-to-docs",
    "title": "Documents from an approved design",
    "description": "Write the HLD + TDD (with an ERD) from a design that is already approved - the approved-design.md a previous run produced, or any design document you name. No re-design, no review loop: one pass over work that has already been signed off.",
    "inputs": [
      {
        "key": "design",
        "label": "The approved design (paste it, and/or attach the file below)",
        "kind": "text",
        "attachTo": true
      },
      {
        "key": "designFile",
        "label": "Path to an approved design file to use as-is (e.g. .dhruva/runs/<run>/docs/approved-design.md) - leave blank to use the text above",
        "kind": "text"
      },
      {
        "key": "docName",
        "label": "Design document name (file-safe)",
        "kind": "text",
        "default": "solution-design"
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot baseline",
        "type": "snapshot"
      },
      {
        "id": "design-in",
        "title": "Take in the approved design (read-only)",
        "type": "agent",
        "prompt": "You are being given a design that has ALREADY been made and signed off. Your only job is to put it into the shape the documents step reads. Do not design anything, do not judge the design, do not add or remove requirements, and do not modify any files.\n\nNormally this step does not run at all: when the person names an approved design file, the engine adopts it directly and nothing is spent here. You are running because no such file was named, so the design arrived as prose - pasted text, an attachment, an older document - and it has to be normalised first.\n\nBinary attachments (.docx/.pdf) cannot be read directly - when one is referenced, read the extracted sibling file <same-name>.extracted.md instead (created at upload); only report a document unreadable if no extracted sibling exists.\n\nRead every attached document in full, in chunks with offsets until the end. A design normalised from a partially read document is a design with requirements missing from it.\n\nThe design:\n{inputs.design}\n\nOutput the design as ONE BLOCK PER REQUIREMENT, wrapped in this fence, with NOTHING else inside it:\n\n=== DESIGN START ===\n(the OVERVIEW paragraph if the source has one, then every requirement block)\n=== DESIGN END ===\n\nEach block formatted exactly:\n### REQ-001: <the requirement's title, in the source's own words>\nBRD-REF: <where it comes from, if the source says; '-' if not>\nSTATUS: ALREADY IMPLEMENTED | PARTIAL | NEW\nEVIDENCE: <as the source gives it; '-' if it gives none>\nALREADY-PRESENT: <as the source gives it; '-'>\nPENDING: <as the source gives it; '-'>\nDESIGN: <the design for this requirement, carried over IN FULL>\nEFFORT: <as the source gives it; '-'>\nDEPENDS-ON: <REQ-ids or '-'>\n\nRules that matter more than tidiness:\n\n- **Carry the text over; do not rewrite it.** This design was reviewed and approved as it stands. Summarising a DESIGN field, tightening its wording, or \"improving\" a decision silently replaces work a human signed with work nobody has seen. Copy it.\n- **Keep the ids and their order exactly as the source has them.** If the source numbers its requirements, those numbers ARE the ids. Never renumber, merge or split.\n- If the source has no ids, number sequentially from REQ-001 in the source's own order, and say in your outcome summary that you assigned them.\n- A field the source does not give is `-`. Do not infer one, and never invent EVIDENCE: a component name you supply here is a claim about an org you have not looked at.\n- If a block carries `HUMAN-NOTE:` or `OPEN-CONFIRMED:`, keep that line verbatim. Those are the human's own words and the questions still outstanding; the documents need both.\n- If what you were given is not a design at all - a raw requirement list, a BRD, an empty file - STOP and say so plainly instead of designing one. This workflow writes documents from an existing design; it does not produce a design.",
        "readOnly": true,
        "orgAware": false,
        "role": "read",
        "artifact": ".dhruva/runs/{runId}/docs/design.md",
        "skipIf": "designFile",
        "timeoutMinutes": 20
      },
      {
        "id": "write-doc",
        "title": "Write HLD + TDD documents (with ERD)",
        "type": "agent",
        "prompt": "The approved design is already written, in full, at .dhruva/runs/{runId}/docs/design.md - one REQ block per requirement, each with STATUS, EVIDENCE, ALREADY-PRESENT, PENDING, DESIGN, EFFORT and DEPENDS-ON. Read it completely. If your search tool reports no match, read the path directly: it is inside a dot-directory, which some search tools skip, and a failed search is not evidence the file is absent.\n\nEach block may also carry its own review history, below a `<!-- lineage -->` marker: the findings raised against it and the designer's answers. That is the record of HOW the design reached its current form - the design itself is the fields ABOVE the marker. Write the documents from the fields. Read the history only when you need to know why something is the way it is, and never carry a superseded design into the documents because it appears in a finding.\n\nIf that file does not exist or has no REQ blocks, STOP and say so plainly - do not invent a design. It is the only copy; writing documents from memory would silently replace an approved design with a guess.\n\nYour job is NOT to restate it. Re-typing a gap analysis that already exists is how requirements get lost: on a real run five of them vanished between the design and the documents. Reference the design for anything it already says, and spend your effort on what it does NOT say.\n\nThe design is organised per REQUIREMENT. These documents are organised per CONCERN - data flow, error handling, governor limits, deployment order. That thinking cuts across all the REQ blocks at once and exists nowhere yet. That is the work.\n\nCreate or modify ONLY these three files:\n\n1. .dhruva/runs/{runId}/docs/hld.md - for stakeholders and review boards:\n  1. Context - the problem space and system boundaries this design stands on.\n  2. Goals / Non-Goals - what it achieves, and what it explicitly does not.\n  3. Gap Analysis - DO NOT re-tabulate the REQ blocks. One short paragraph of the shape of the gap (how many already implemented / partial / new, and the theme), then: \"Per-requirement detail: see design.md.\"\n  4. Approaches Considered - assemble this from the REJECTED lines the design already recorded: per decision, the option taken, the option rejected, and the constraint that ruled it out. Do NOT invent alternatives the design does not name - a deliberation nobody had, written up after approval, is the one section of this document no reviewer ever checked. Add the overall complexity (XS/S/M/L/XL) for the selected approach.\n  5. High-Level Design - how the component groups interact, and the key abstractions introduced. Architecture, not a per-REQ list.\n  6. Data Model - a Mermaid er-diagram (```mermaid / erDiagram) of new and impacted objects, their relationships and key fields.\n  7. Integration Touchpoints and Security Model.\n  8. Constraints and Trade-offs - assemble from the design's TRADE-OFF lines; add only constraints that emerge across requirements rather than within one.\n  9. Acceptance Criteria - \"AC-n: Given <precondition>, when <action>, then <outcome>. [traces: REQ-xxx]\". Every REQ with pending work needs at least one AC.\n  10. Impact analysis, risks and assumptions, Open Questions. For effort, total the design's EFFORT values and show the total plus any contingent items - do not re-list every requirement.\n\n2. .dhruva/runs/{runId}/docs/tdd.md - for the developers who build it:\n  1. Components - per apex class, trigger, LWC, flow, object and field: exact API names, responsibility, inputs/outputs, dependencies, reuse-vs-new, and method-level design or pseudocode where non-trivial.\n  2. Data Flow - how data moves end to end.\n  3. State Management - what state exists, where it lives, how it changes.\n  4. Error Handling - what can fail, and how each failure is handled and surfaced.\n  5. Governor Limits and Sharing/FLS specifics.\n  6. Deployment Order and dependencies.\n  7. Test Strategy - the test classes and scenarios (positive, negative, bulk). Apex tests run in the org, so tests ship WITH the implementation and are proven by a check-only deploy with RunLocalTests; never promise a failing-tests-first cycle.\n  8. Build Plan - a short summary table of the tasks file (T-n | title | depends on | traces). The tasks file is the authority.\n  9. Decisions - one line each: Decision -> Rationale -> Consequence.\n  10. Open Questions that may affect implementation.\n\n3. .dhruva/runs/{runId}/docs/tasks.json - the machine-readable build plan the implementation workflow executes. Strict JSON, no comments, no trailing commas, exactly this shape:\n{ \"version\": 1, \"tasks\": [ { \"id\": \"T-1\", \"title\": \"<imperative, one line>\", \"depends_on\": [], \"files\": [\"force-app/main/default/classes/Example.cls\"], \"change\": \"<the mechanism - what edit, where>\", \"test_scenarios\": [\"<case>\"], \"traces\": [\"REQ-001\", \"AC-1\"], \"status\": \"pending\" } ] }\nRules: ids sequential from T-1; every task lists the project-relative files it touches and traces to at least one REQ or AC; depends_on names only earlier tasks; no cycles; one component plus its test class per task; order is safe deployment order. Take file paths from the design's EVIDENCE where it names them - guess a path only when the design gives none, and follow this project's existing folder layout when you do.\n\nDo not contradict the approved design. Where you need precision it does not give, add it; where it already decided something, cite it rather than re-deciding.\n\nThe Mermaid block must be valid erDiagram syntax - every relationship line has both entities and a label - with no styling directives.\n\nAPPROVED UX DESIGN: if .dhruva/runs/{runId}/docs/ux-design.md exists, read it completely and incorporate it fully (when UX is disabled for this project the file is absent - then skip this paragraph). The TDD gains a 'UI Components' section carrying every UX-n block, and the tasks file gets one task per UX component (files under force-app/main/default/lwc/..., traces include the UX-n id AND its REQ id).",
        "persona": "salesforce-writer",
        "role": "design",
        "timeoutMinutes": 30
      },
      {
        "id": "changes",
        "title": "Collect created documents",
        "type": "changes"
      },
      {
        "id": "approve-doc",
        "title": "Accept the design documents",
        "type": "gate",
        "message": "HUMAN GATE - you are shown the coverage verdict and any unresolved findings; the documents are in .dhruva/runs/{runId}/docs/. If items are MISSING or DIVERGE, revise; accept to complete the run.",
        "reviseTarget": "write-doc"
      }
    ]
  },
  "feature-dev": {
    "id": "feature-dev",
    "title": "Feature development",
    "description": "Turn a requirement into a technical spec (gated), implement with tests, review the diff, validate and deploy.",
    "inputs": [
      {
        "key": "requirement",
        "label": "Requirement / user story",
        "kind": "text",
        "attachTo": true
      },
      {
        "key": "runTests",
        "label": "Run local Apex tests during validation",
        "kind": "boolean",
        "default": true
      },
      {
        "key": "deploy",
        "label": "Deploy to the default org at the end",
        "kind": "boolean",
        "default": false
      },
      {
        "key": "visualTest",
        "label": "Visual test before deploy (opens the org in the browser with your LOCAL UI files + live org data)",
        "kind": "boolean",
        "default": false
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot baseline",
        "type": "snapshot"
      },
      {
        "id": "spec",
        "title": "Draft technical spec (agent, read-only)",
        "type": "agent",
        "prompt": "Binary attachments (.docx/.pdf) cannot be read directly - when one is referenced, read the extracted sibling file <same-name>.extracted.md instead (created at upload); only report a document unreadable if no extracted sibling exists.\nYou are designing a Salesforce implementation for a requirement. DO NOT modify any files in this step.\nRequirement: {inputs.requirement}\nStudy the existing codebase first (reuse existing classes/objects/patterns - never plan a parallel implementation of something that exists).\nReply in EXACTLY this structure:\n1. USE CASES - extract them from the requirement with stable ids:\n   UC-1: <title> - <actor> wants to <action> so that <outcome>.  (happy paths)\n   UC-E1: <title> - what happens when <error/edge condition>.    (error & edge cases - think bulk loads, missing permissions, null data, concurrent edits)\n2. SCOPE - 'In scope' bullet list and 'Out of scope' bullet list (with why), so the reviewer sees the boundary explicitly.\n3. TECHNICAL SPEC - (a) approach, (b) components to create or modify with exact API names, (c) test plan covering every UC id above.\n4. OPEN QUESTIONS - anything unresolved that could change the design ('none' if none).\nEnd your reply with one line listing every EXISTING file you will modify as project-relative paths:\nFILES: force-app/main/default/classes/Example.cls",
        "readOnly": true,
        "role": "design",
        "emits": "work"
      },
      {
        "id": "approve-spec",
        "title": "Approve technical spec",
        "type": "gate",
        "message": "Review the technical spec above. Proceed with implementation?"
      },
      {
        "id": "work-check",
        "title": "Anything left to build?",
        "type": "work-check"
      },
      {
        "id": "retrieve-fresh",
        "title": "Retrieve fresh copies of files to be modified",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "retrieve",
          "start",
          "{affectedSourceDirs}",
          "--json",
          "--wait",
          "10"
        ],
        "optional": true
      },
      {
        "id": "retrieve-delta",
        "title": "Compare retrieved files against spec-time versions",
        "type": "changes"
      },
      {
        "id": "rebaseline",
        "title": "Re-baseline after org refresh",
        "type": "snapshot"
      },
      {
        "id": "implement",
        "title": "Implement feature + tests (agent)",
        "type": "agent",
        "prompt": "Implement this feature in the current project.\nRequirement: {inputs.requirement}\nApproved technical spec:\n{steps.spec.output}\n\nOrg-refresh delta since the spec was written:\n{steps.retrieve-delta.output}\nIf files are listed in that delta, re-read them and adapt the spec before coding.\nWrite or update Apex tests for everything you implement (aim for the changed classes to be covered). Follow the existing code style. Never create a parallel implementation of something that already exists. Do not deploy.",
        "role": "implement"
      },
      {
        "id": "changes",
        "title": "Collect changed files",
        "type": "changes"
      },
      {
        "id": "verify-standards",
        "title": "Verify standards on changed files (deterministic)",
        "type": "verify"
      },
      {
        "id": "review",
        "title": "Code review of the changes (agent, read-only)",
        "type": "agent",
        "prompt": "Review ONLY the changes listed below (made in this run) against the team standards. Do not modify any files.\nChanged files:\n{steps.changes.output}\nDeterministic standards-check result:\n{steps.verify-standards.output}\nRead each changed file and review the actual change.",
        "readOnly": true,
        "persona": "salesforce-review",
        "role": "review",
        "autoRevise": {
          "target": "implement",
          "trigger": "VERDICT:\\s*BLOCKED",
          "maxRounds": 3
        },
        "emits": "findings"
      },
      {
        "id": "traceability",
        "title": "Requirement coverage check (agent, read-only)",
        "type": "agent",
        "prompt": "Binary attachments (.docx/.pdf) cannot be read directly - when one is referenced, read the extracted sibling file <same-name>.extracted.md instead (created at upload); only report a document unreadable if no extracted sibling exists.\nCoverage check. Re-read the full requirement (and any attached documents it references):\n{inputs.requirement}\nThe approved spec extracted numbered use cases (UC-n / UC-En):\n{steps.spec.output}\nFor EACH UC id in the spec (and any requirement item the spec missed), inspect the changed files below and report one line:\n  UC-x: <title> - IMPLEMENTED (evidence: file + method/element) | PARTIAL (what is missing) | MISSING\nChanged files in this run:\n{steps.changes.output}",
        "readOnly": true,
        "role": "trace",
        "emits": "coverage"
      },
      {
        "id": "approve-changes",
        "title": "Approve code changes",
        "type": "gate",
        "message": "HUMAN GATE - you are shown the diffs, the code reviewer's verdict with its findings, and the coverage check. If findings remain or items are PARTIAL/MISSING, revise (e.g. 'implement items 2 and 5'); approve to validate against the org.",
        "reviseTarget": "implement"
      },
      {
        "id": "validate",
        "title": "Validate (check-only deploy of changed files)",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "{changedSourceDirs}",
          "--json",
          "--wait",
          "30"
        ]
      },
      {
        "id": "validate-tests",
        "title": "Validate with local tests",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "{changedSourceDirs}",
          "--test-level",
          "RunLocalTests",
          "--json",
          "--wait",
          "60"
        ],
        "onlyIf": "runTests"
      },
      {
        "id": "visual-preview",
        "title": "Visual test - local files against live org data (Local Dev)",
        "type": "cli",
        "bin": "sf",
        "args": [
          "lightning",
          "dev",
          "app"
        ],
        "optional": true,
        "detached": true,
        "onlyIf": "visualTest"
      },
      {
        "id": "approve-deploy",
        "title": "Approve deploy",
        "type": "gate",
        "message": "Validation passed. If you enabled the visual test, click through the app in the browser first. Deploy the changed files to the default org now?",
        "onlyIf": "deploy"
      },
      {
        "id": "deploy",
        "title": "Deploy changed files",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "{changedSourceDirs}",
          "--json",
          "--wait",
          "30"
        ],
        "onlyIf": "deploy"
      }
    ]
  },
  "implement-tdd": {
    "id": "implement-tdd",
    "title": "Implement from TDD",
    "description": "Build what an approved Technical Design Document specifies: plan gate, fresh org copies, implementation with tests, standards check, review, validate, deploy to the connected sandbox.",
    "inputs": [
      {
        "key": "tddPath",
        "label": "TDD path (from a Solution design run - see .dhruva/runs/<runId>/docs/)",
        "kind": "text",
        "default": ""
      },
      {
        "key": "tasksPath",
        "label": "Build-plan tasks file from the same run (blank = single-pass build)",
        "kind": "text",
        "default": ""
      },
      {
        "key": "scope",
        "label": "Scope note (optional - e.g. only components 1-3)",
        "kind": "text",
        "default": "",
        "attachTo": true
      },
      {
        "key": "runTests",
        "label": "Run local Apex tests during validation",
        "kind": "boolean",
        "default": true
      },
      {
        "key": "deploy",
        "label": "Deploy to the connected sandbox at the end",
        "kind": "boolean",
        "default": true
      },
      {
        "key": "visualTest",
        "label": "Visual test before deploy (opens the org in the browser with your LOCAL UI files + live org data)",
        "kind": "boolean",
        "default": false
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot baseline",
        "type": "snapshot"
      },
      {
        "id": "plan",
        "title": "Read the TDD and plan the build (agent, read-only)",
        "type": "agent",
        "prompt": "Read the approved Technical Design Document at {inputs.tddPath} in this project. DO NOT modify any files in this step.\nScope note from the requester (may narrow the work): {inputs.scope}\nCross-check the TDD against the current codebase (components it references, reuse it assumes). If the TDD has a Build Plan section (task table T-1, T-2… with depends_on), use ITS order as the checklist; otherwise produce the implementation checklist in dependency order yourself. Either way: each component to create or modify, and flag anything in the TDD that no longer matches the codebase.\nEnd with one line listing every EXISTING file you will modify as project-relative paths:\nFILES: force-app/main/default/classes/Example.cls",
        "readOnly": true,
        "role": "read",
        "emits": "work"
      },
      {
        "id": "approve-plan",
        "title": "Approve the build plan",
        "type": "gate",
        "message": "Review the implementation checklist above (revise with instructions if the order or scope needs changing). Proceed with the build?"
      },
      {
        "id": "work-check",
        "title": "Anything left to build?",
        "type": "work-check"
      },
      {
        "id": "retrieve-fresh",
        "title": "Retrieve fresh copies of files to be modified",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "retrieve",
          "start",
          "{affectedSourceDirs}",
          "--json",
          "--wait",
          "10"
        ],
        "optional": true
      },
      {
        "id": "retrieve-delta",
        "title": "Compare retrieved files against plan-time versions",
        "type": "changes"
      },
      {
        "id": "rebaseline",
        "title": "Re-baseline after org refresh",
        "type": "snapshot"
      },
      {
        "id": "implement",
        "title": "Implement per TDD (agent - one bounded run per build-plan task)",
        "type": "agent",
        "prompt": "Implement the approved plan. The Technical Design Document at {inputs.tddPath} is the specification - follow its component designs, API names, and test strategy exactly.\nApproved build checklist:\n{steps.plan.output}\n\nOrg-refresh delta since planning:\n{steps.retrieve-delta.output}\nIf files are listed in that delta, re-read them before changing them.\nWrite or update the Apex tests the TDD's test strategy specifies. Never create a parallel implementation of something that exists. Do not deploy.",
        "role": "implement",
        "taskLoop": true,
        "timeoutMinutes": 30,
        "tasksFile": "{inputs.tasksPath}"
      },
      {
        "id": "changes",
        "title": "Collect changed files",
        "type": "changes"
      },
      {
        "id": "verify-standards",
        "title": "Verify standards on changed files (deterministic)",
        "type": "verify"
      },
      {
        "id": "review",
        "title": "Code review against the TDD (agent, read-only)",
        "type": "agent",
        "prompt": "Review ONLY the changes listed below against the team standards AND against the TDD at {inputs.tddPath} (the changes must implement what the TDD specifies - flag deviations). Do not modify any files.\nChanged files:\n{steps.changes.output}\nDeterministic standards-check result:\n{steps.verify-standards.output}",
        "readOnly": true,
        "persona": "salesforce-review",
        "role": "review",
        "autoRevise": {
          "target": "implement",
          "trigger": "VERDICT:\\s*BLOCKED",
          "maxRounds": 3
        },
        "emits": "findings",
        "tasksFile": "{inputs.tasksPath}"
      },
      {
        "id": "traceability",
        "title": "Requirements traceability matrix (agent, read-only)",
        "type": "agent",
        "prompt": "Build a requirements traceability matrix. Re-read the ENTIRE TDD at {inputs.tddPath} (chunked reads to the end). Extract every requirement/component/behavior it specifies (respect the scope note: {inputs.scope}). For EACH item, inspect the changed files below and report one line:\n  <n>. <requirement item> - IMPLEMENTED (evidence: file + method/element) | PARTIAL (what is missing) | MISSING\nChanged files in this run:\n{steps.changes.output}",
        "readOnly": true,
        "role": "trace",
        "emits": "coverage"
      },
      {
        "id": "approve-changes",
        "title": "Approve code changes",
        "type": "gate",
        "message": "HUMAN GATE - you are shown the diff, the code reviewer's verdict with its findings (reopened tasks already carry the reviewer's work orders), and the traceability matrix. Revise to rework, approve to validate against the org.",
        "reviseTarget": "implement"
      },
      {
        "id": "validate",
        "title": "Validate (check-only deploy of changed files)",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "{changedSourceDirs}",
          "--json",
          "--wait",
          "30"
        ]
      },
      {
        "id": "validate-tests",
        "title": "Validate with local tests",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "{changedSourceDirs}",
          "--test-level",
          "RunLocalTests",
          "--json",
          "--wait",
          "60"
        ],
        "onlyIf": "runTests"
      },
      {
        "id": "visual-preview",
        "title": "Visual test - local files against live org data (Local Dev)",
        "type": "cli",
        "bin": "sf",
        "args": [
          "lightning",
          "dev",
          "app"
        ],
        "optional": true,
        "detached": true,
        "onlyIf": "visualTest"
      },
      {
        "id": "approve-deploy",
        "title": "Approve deploy to the connected sandbox",
        "type": "gate",
        "message": "Validation passed. If you enabled the visual test, click through the app in the browser first. Deploy the changed files to the connected sandbox now?",
        "onlyIf": "deploy"
      },
      {
        "id": "deploy",
        "title": "Deploy changed files",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "{changedSourceDirs}",
          "--json",
          "--wait",
          "30"
        ],
        "onlyIf": "deploy"
      }
    ]
  },
  "retrieve-sync": {
    "id": "retrieve-sync",
    "title": "Retrieve / org sync",
    "description": "Pull the org's current metadata into the local folder and show exactly what drifted since the last local state.",
    "inputs": [
      {
        "key": "target",
        "label": "Source path to sync (e.g. force-app)",
        "kind": "text",
        "default": "force-app"
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot current local state",
        "type": "snapshot"
      },
      {
        "id": "retrieve",
        "title": "Retrieve from org",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "retrieve",
          "start",
          "--source-dir",
          "{inputs.target}",
          "--json",
          "--wait",
          "60"
        ]
      },
      {
        "id": "drift",
        "title": "Org drift vs previous local state",
        "type": "changes"
      }
    ]
  },
  "run-tests": {
    "id": "run-tests",
    "title": "Run Apex tests",
    "description": "Run Apex tests in the default org with code coverage.",
    "inputs": [
      {
        "key": "level",
        "label": "Test level",
        "kind": "select",
        "options": [
          "RunLocalTests",
          "RunAllTestsInOrg",
          "RunSpecifiedTests"
        ],
        "default": "RunLocalTests"
      },
      {
        "key": "tests",
        "label": "Class names (only for RunSpecifiedTests, comma-separated)",
        "kind": "text",
        "default": ""
      },
      {
        "key": "synchronous",
        "label": "Run synchronously (avoids UNABLE_TO_LOCK_ROW when tests contend on shared records; slower)",
        "kind": "boolean",
        "default": false
      }
    ],
    "steps": [
      {
        "id": "run",
        "title": "Run tests",
        "type": "cli",
        "bin": "sf",
        "args": [
          "apex",
          "run",
          "test",
          "--test-level",
          "{inputs.level}",
          "{opt:--tests:inputs.tests}",
          "{flag:--synchronous:inputs.synchronous}",
          "--code-coverage",
          "--result-format",
          "json",
          "--json",
          "--wait",
          "60"
        ]
      }
    ]
  },
  "scratch-org": {
    "id": "scratch-org",
    "title": "Scratch org from this folder",
    "description": "Create a scratch org (needs an authorized Dev Hub), push this folder's source into it, and open it in the browser.",
    "inputs": [
      {
        "key": "alias",
        "label": "Scratch org alias",
        "kind": "text",
        "default": "scratch1"
      },
      {
        "key": "days",
        "label": "Duration (days)",
        "kind": "select",
        "options": [
          "1",
          "7",
          "14",
          "30"
        ],
        "default": "7"
      }
    ],
    "steps": [
      {
        "id": "confirm",
        "title": "Confirm scratch org creation",
        "type": "gate",
        "message": "This creates a scratch org from config/project-scratch-def.json (consumes one of your Dev Hub's daily scratch orgs) and pushes force-app into it. Proceed?"
      },
      {
        "id": "create",
        "title": "Create scratch org",
        "type": "cli",
        "bin": "sf",
        "args": [
          "org",
          "create",
          "scratch",
          "--definition-file",
          "config/project-scratch-def.json",
          "--alias",
          "{inputs.alias}",
          "--duration-days",
          "{inputs.days}",
          "--json",
          "--wait",
          "20"
        ]
      },
      {
        "id": "push",
        "title": "Push local source into the scratch org",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--source-dir",
          "force-app",
          "--target-org",
          "{inputs.alias}",
          "--json",
          "--wait",
          "60"
        ]
      },
      {
        "id": "open",
        "title": "Open the scratch org in the browser",
        "type": "cli",
        "bin": "sf",
        "args": [
          "org",
          "open",
          "--target-org",
          "{inputs.alias}"
        ]
      }
    ]
  },
  "solution-design": {
    "id": "solution-design",
    "title": "Solution design",
    "description": "Architect path: extract the requirement list from the source documents, design each requirement against the existing codebase, gate on the proposed design, then produce HLD + TDD documents (with an ERD) in the project.",
    "inputs": [
      {
        "key": "requirement",
        "label": "Requirement (paste the text and/or attach documents below)",
        "kind": "text",
        "attachTo": true
      },
      {
        "key": "attachedIsRequirements",
        "label": "The attached file IS the requirements list (skip extraction)",
        "kind": "boolean"
      },
      {
        "key": "docName",
        "label": "Design document name (file-safe)",
        "kind": "text",
        "default": "solution-design"
      },
      {
        "key": "uxEnabled",
        "label": "UX design (from project settings)",
        "kind": "boolean",
        "hidden": true
      },
      {
        "key": "uxRules",
        "label": "UX rules (from project settings)",
        "kind": "text",
        "hidden": true
      },
      {
        "key": "designDir",
        "label": "Design folder (from project settings)",
        "kind": "text",
        "hidden": true
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot baseline",
        "type": "snapshot"
      },
      {
        "id": "requirements",
        "title": "Extract the requirement list from the source documents (read-only)",
        "type": "agent",
        "prompt": "Read the source documents and extract the requirement list. Do NOT design anything, do not inventory the codebase, and do not modify any files.\n\nThis list is written ONCE and then frozen. Every later step cites these ids: the design produces one block per REQ id, the reviewer checks coverage against this list, and the documents trace back to it. Nothing downstream re-reads the source document, so a requirement you leave out here is a requirement that never gets built.\n\nThat is also why the ids must never move. The same source document has produced 11, then 16, then 15 requirements across three runs of the old pipeline, because nobody ever wrote down what a requirement WAS. You are writing that down.\n\nBinary attachments (.docx/.pdf) cannot be read directly - when one is referenced, read the extracted sibling file <same-name>.extracted.md instead (created at upload); only report a document unreadable if no extracted sibling exists.\n\nRead EVERY attached document in full - every page and line, continuing in chunks with offsets until the end of each file. A list built from a partially read document is invalid; if a document cannot be fully read, say so explicitly instead of proceeding.\n\nRequirement:\n{inputs.requirement}\n\n## Scope: what the requirement text says, wins\n\nThe text above is the person's instruction, and it OUTRANKS the \"extract everything\" rules below. If it narrows the job - \"only user stories 1 and 2\", \"just Feature 4\", \"ignore the integration sections, that system is not built yet\", \"skip anything to do with reporting\" - then obey it exactly:\n\n- Extract ONLY what is in scope. Number it normally from REQ-001; the ids describe this run, not the document.\n- **Record what you left out, and why.** Close the list with an `## EXCLUDED` section naming each part of the document you did not extract and the instruction that excluded it. Something deliberately out of scope must never look like something you missed - a later reader, and the coverage check, both need to tell the difference.\n- Do not narrow the scope on your own. Only an explicit instruction takes something out; a section that merely looks difficult, incomplete, or out of date stays in.\n- If the instruction is ambiguous about a section, keep the section and note the ambiguity in its `OPEN:` line rather than guessing.\n\nWhere the text says nothing about scope, extract the whole document as described below.\n\nOutput a title line, then ONE BLOCK PER REQUIREMENT, in the source document's own order, formatted exactly:\n\n### REQ-001: <short title, in the document's own words>\nSOURCE: <where it comes from: feature / user story / section and page>\nASKS:\n- AC1: <the acceptance criterion, in the document's wording>\n- AC2: <...>\nOPEN: <a decision the document itself leaves undecided (\"to be finalized during development\"), or '-'>\n\nRules:\n- Number sequentially from REQ-001 with no gaps.\n- One block per distinct ask. Do not merge unrelated asks to keep the list short, and do not split one ask into several to make it look thorough.\n- List EVERY acceptance criterion the document states, each on its own AC line, using the document's wording rather than your paraphrase. These are what the design is later checked against, one by one.\n- Use the document's EXACT names for user-facing actions, labels and fields. A renamed label is a scope change, and it will be built the way you write it here.\n- Carry an undecided decision into OPEN rather than settling it. You are recording the requirement, not choosing the solution.\n- Where the document assigns work to another system, say so in the block instead of dropping it - it is a requirement about a boundary, and the design has to acknowledge it.\n- Do not add requirements the document does not state, and do not judge whether anything is already built. That is the design step's job, against the codebase.\n- Do not raise setup or deployment actions. You have not seen this org, so you cannot know whether a site is already published, a permission set already exists, or a mapping table is already loaded. The design step reads the codebase and raises those; a checklist written from the document alone sends the customer work they have already done.\n\nClose with one line: `TOTAL: <n> requirements, <m> acceptance criteria.` - counting what is IN scope.\n\nThen, only if the requirement text narrowed the job, the exclusions:\n\n## EXCLUDED\n- <the feature, user story or section left out> - <the instruction that excluded it>\n\nEverything downstream treats this list as the whole job: the design produces one block per REQ id here, the reviewer checks coverage against it, and the documents trace back to it. Nothing re-reads the source document. So a section you exclude is genuinely not designed, not built, and not costed - which is exactly right when it was asked for, and a silent hole when it was not.",
        "readOnly": true,
        "orgAware": false,
        "role": "read",
        "artifact": ".dhruva/runs/{runId}/docs/requirements.md",
        "skipIf": "attachedIsRequirements",
        "timeoutMinutes": 30
      },
      {
        "id": "analyse",
        "title": "Design per requirement against the codebase (architect, read-only)",
        "type": "agent",
        "prompt": "The DESIGN STATE block above says whether you are AUTHORING or REVISING. When a design exists, the document at the END of this prompt is your working copy: blocks in play in full, settled unrelated blocks as contract stubs. Do not search the filesystem for design.md, and never treat an empty search as proof no design exists (a run that did so destroyed three rounds of accepted fixes).\n\n# If DESIGN STATE says REVISING\n\nYou are editing a living document the engine owns. You do not rewrite it; you send it changes. Output NOTHING but a delta, fenced exactly like this:\n\n=== DELTA START ===\nUNCHANGED: REQ-001, REQ-002, REQ-004\n### REQ-003: <the block's existing title>\nDESIGN: <only the fields you are changing, in the same field format as below>\nEFFORT: 3d\nRESPONSE F24 CHECKED: <the file and line, or BRD section, you actually opened, and what was there>\nRESPONSE F24 ACCEPTED - FIXED: <what you changed>\n=== DELTA END ===\n\n- Include ONLY blocks you changed or are answering a finding on; each requirement at most ONCE, final form only. Everything outside the fence is discarded.\n- **A field you send REPLACES that field completely, so it must be complete and standalone. Omitting a field is how you say it is unchanged.** Never write \"as previously designed\", \"(unchanged ...)\", \"as above\", or any pointer to text you are not including - such a field is refused and the old one kept, so your change simply does not land (one such revision would have erased 21 of 34 blocks).\n- Fields and RESPONSE lines may be in any order. Never renumber or invent a requirement id - unknown ids are dropped and reported.\n- Do not reproduce review history, STATE lines, or anything below a `<!-- lineage -->` marker - the engine owns those; you cannot lose them and must not copy them.\n- Blocks you leave out are carried through untouched.\n\n**A requirement is not done until EVERY id on its `OPEN FINDINGS:` line is answered** - fixed, rejected with evidence, or PARTIAL with what remains - and your fix to one must not break another. An unanswered id sends the block straight back next round regardless of your other fixes.\n\nWhat each STATE means for you:\n- **open** - judge every finding on the block, fix or defend, and emit the block.\n- **open - held by its own OPEN-CONFIRMED, not by a finding** - YOUR unanswered question holds it. Settle it only if you can now name the file and line that answers it (replace the line with `OPEN-RESOLVED:`); otherwise leave it exactly as is for the human at the gate. Never quietly pick an answer to clear the state.\n- **clean** - leave it out entirely. One exception: if fixing an open block forces a change here, emit it and name the finding that forced it.\n- **approved** - leave it out entirely.\n- **approved - reviewer objects** - RESPOND, do not rewrite: a human approved it, so only a human may change it. Emit RESPONSE lines and NO design fields, using ACCEPTED - WOULD FIX where you would otherwise fix. The human rules at the next gate.\n\n## Judging a finding\n\nA finding is a reviewer's claim, not an instruction - it can be wrong about this codebase or the BRD. A wrong claim ACCEPTED writes a falsehood into the design that every later round inherits, which is worse than a finding missed.\n\n- Open the file the finding cites. CHECKED must name the file and line you actually opened, and what was there.\n- Test the finding's SCOPE, not just its example: \"grants Read on seven objects\" once turned out to be View All Records on 399, and the design was rewritten around the wrong claim. True-about-its-example, wrong-about-its-reach is a wrong finding.\n- Exactly one verdict per finding:\n    RESPONSE <id> ACCEPTED - FIXED: what you changed\n    RESPONSE <id> REJECTED: the evidence that the finding's premise does not hold\n    RESPONSE <id> PARTIAL: what you fixed, what you did not, and why\n    RESPONSE <id> ACCEPTED - WOULD FIX: approved blocks only - what you would change, for the human to decide\n- ACCEPTED and REJECTED are equally valid. Never accept a finding you could not verify - say REJECTED or PARTIAL and show what you looked at. Rejecting nothing across ten findings is itself a signal you did not check.\n- Your REJECTED stays in the document; the next reviewer must answer it instead of re-raising it.\n\n# If DESIGN STATE says AUTHORING\n\nWrap the finished design in this fence, and put NOTHING else inside it:\n\n=== DESIGN START ===\n(the OVERVIEW paragraph, then every requirement block)\n=== DESIGN END ===\n\nEverything outside the fence is discarded - investigate and think aloud freely above it - but each requirement appears EXACTLY ONCE inside, final form (a run that left drafts inside shipped 86 headings for 34 requirements and spent four review rounds on the contradictions).\n\nA requirement needs a solution design for THIS org's codebase. DO NOT modify any files in this step.\n\nThe requirement list below is FROZEN: one block per REQ id, ids and order exactly, never renumber or merge. Do not re-read the source document to second-guess it - if an item is genuinely unusable, say so in its block rather than quietly redefining it.\n\nFROZEN REQUIREMENT LIST:\n{steps.requirements.output}\n\nInvestigate THIS codebase yourself, per requirement: objects and fields, automation (triggers/flows), apex services, LWCs, permission sets. There is deliberately no pre-gathered inventory - summaries nobody re-checked have put methods on wrong classes and missed live features. Open the file. What you cite, you have read.\n\nThen output in EXACTLY this structure (it is machine-parsed into review cards):\n\nFirst a short OVERVIEW paragraph (overall approach, phasing, key risks).\n\nThen ONE BLOCK PER REQUIREMENT, in the frozen list's order, each formatted exactly:\n### REQ-001: <the title from the frozen list>\nBRD-REF: <the SOURCE line from the frozen list, plus the AC numbers this block covers>\nSTATUS: ALREADY IMPLEMENTED | PARTIAL | NEW\nEVIDENCE: <for implemented/partial: the exact existing components, backticked API names, each with the file and line you opened>\nALREADY-PRESENT: <what exists today; '-' for NEW>\nPENDING: <what is missing; '-' if nothing>\nDESIGN: <the solution for exactly this item's PENDING work - components to create/modify with API names, declarative vs code and why, security notes. For ALREADY IMPLEMENTED items write 'No work required' plus any caveat.>\nREJECTED: <the alternative you seriously weighed and did NOT take, and the constraint that ruled it out - e.g. \"Before-Save flow: cannot issue the callout\". '-' when there was genuinely only one buildable option, which is itself worth saying.>\nTRADE-OFF: <what this choice gives up and why that is acceptable here; '-' if nothing material.>\nEFFORT: <rough estimate, e.g. 2d>\nDEPENDS-ON: <REQ-ids or '-'>\n\nEVIDENCE is the load-bearing field: the single largest class of review finding is this design asserting something about the org that is not true. Opening the file is not enough - **verify the specific property your design depends on**:\n\n| If your design says | Check, and state what you found |\n|---|---|\n| \"reuse `X.method()`\" | the method exists with that signature; something actually CALLS it (dead code has carried three designs); read the body |\n| \"add field `Y__c`\" | `Y__c` does not already exist; nothing already populates it; the type supports your use |\n| \"write to `Z__c`\" | it is WRITABLE - a formula field cannot be assigned |\n| \"compare A to B\" | the two are comparable - a Lookup Id does not equal free text; a picklist value you name must be in the picklist |\n| \"this is missing\" | it is genuinely absent - Export, filters and history tabs have all been designed as new while already implemented |\n| \"nothing grants / no file mentions X\" | you RAN that search and this is what it returned - a negative is a claim about every file (a security model once rested on \"no profile mentions\" an object two profiles granted) |\n| \"pass `attr` to `<existing-lwc>`\" | that `@api` exists - read the component's own `@api` list |\n\nEVERY name in EVIDENCE must exist in this project TODAY - the engine checks each against the repository and reports misses. Something you intend to CREATE belongs in DESIGN, never EVIDENCE.\n\nRead your blocks against each other before you finish: reviewers have caught one field given two definitions, contradictory eligibility rules, two names for one engine, and a DEPENDS-ON cycle. If two blocks touch the same object, field or class, say the same thing in both.\n\nStay on the DESIGN. Tests, code style and build mechanics belong to the implementation workflow - do not name test classes here. EFFORT covers the whole work; the block's substance is what to build and why.\n\nEvery AC in a requirement's frozen block must be answered by that block - covered by DESIGN, or in ALREADY-PRESENT with evidence. An AC neither designed nor accounted for is a requirement dropped.\n\nYou are the step that CHOOSES, so you record what was rejected - inventing the deliberation later means documenting a comparison nobody performed or checked.\n\nRules: one block per REQ id, ids and order unchanged; STATUS strict (ALREADY IMPLEMENTED needs concrete component evidence AND `EFFORT: 0` - ANY remaining work, even hardening, makes it PARTIAL with that work in PENDING; a caveat may be an observation, never work); design ONLY the pending work; follow the team standards in this prompt.\n\nRESOLVE EVERY `OPEN`. The requirement step read the source document only - never this org - so each `OPEN` is a question about the DOCUMENT. You are the first step with the codebase, so you settle which are real. For every `OPEN` on a requirement, add one line to its block:\n\nOPEN-RESOLVED: <the file and line that answers it, and the answer>\nOPEN-CONFIRMED: <why the codebase cannot settle it, and who must decide - a business decision, an external contract, or missing data>\n\nOnly an `OPEN-CONFIRMED` reaches the client. Escalating one this org already answers asks the customer something you could have read in their own code. Design an OPEN-CONFIRMED so either outcome is a cheap swap, and never silently settle it.\n\nBRD FIDELITY (violations fail UAT): use the frozen list's EXACT names for user-facing actions/labels/fields - renaming is a scope change; never add integrations, jobs, or behaviors the requirement assigns to another system - mark them as open contract decisions instead.",
        "readOnly": true,
        "persona": "salesforce-architect",
        "role": "design",
        "artifact": ".dhruva/runs/{runId}/docs/design.md",
        "timeoutMinutes": 45
      },
      {
        "id": "design-review",
        "title": "Design critique (best model, read-only) - auto-fixes up to 3 times before your gate",
        "type": "agent",
        "prompt": "You are the DESIGN REVIEWER. Adversarially critique the solution design below before a human sees it. Do not modify any files - report everything in your answer and the engine records it.\n\nThe design is quoted in full below - do not go looking for another copy. From round 2 onward a **CHANGED SINCE YOUR LAST REVIEW** block sits above it (every field that moved, WAS and NOW, and the findings each change answered). Read it first: it is how you tell a fix that landed from one that was only described - a response claiming a change that does not appear there is a finding in itself. Each block carries its history underneath; read them together.\n\n**Round 2+ is a re-review, not a fresh audit**: judge the changed blocks and everything DEPENDS-ON connects to them (both directions), verify the fixes, and hunt NEW problems the changes introduced. Do not re-litigate blocks that are unchanged and previously clean - unless a change ripples into them, which is exactly what you are here to catch.\n\n## Before you raise anything new\n\n1. **Give a status to every open finding** (each requirement's `OPEN FINDINGS:` line names its own). Keep each id and say exactly one of RESOLVED, STILL OPEN or PARTIAL, with one line of why. Never renumber; never leave one unmentioned - silence is not a status, and an unmentioned finding stays open and comes back.\n\n   **A requirement clears only when every finding on it clears.** Judge one at a time: a good fix for one finding must not carry the other two, and a resolved finding must not be held open by an unrelated bad one on the same block.\n2. **Audit your own earlier findings.** If one was wrong - wrong file, wrong scope, wrong premise - retract it plainly rather than letting the design carry a fix built on it (one run reported the same fact three times with three different numbers and never retracted).\n3. **Answer a REJECTED before re-raising it.** Accept the rejection, or rebut it with NEW evidence you read yourself - file and line.\n4. **Check changed blocks against unchanged ones** - a fix in one requirement routinely invalidates an assumption in another; follow DEPENDS-ON both ways. `approved` blocks are still yours to judge: approval is the human's decision to proceed, not a correctness claim.\n\nThen add genuinely new findings, numbered after the highest id in use.\n\n## Round 1 is the round that matters\n\nEvery finding you will ever raise, raise NOW. A defect first reported in round 3 was already in the design in round 1 - a late finding is a wasted round, and there are only three. (Measured: a round-2 batch of four findings against a design changed in ZERO blocks - including the run's largest, a security claim wrong across fourteen requirements.)\n\n**The shape to hunt: NEGATIVE claims.** \"No file in `profiles/` mentions this object\", \"the grep returns nothing\", \"the component exposes no such property\" - each reads as diligence done, each asserts something about ALL of something, and each of those three was false. You cannot confirm a negative by reading the block - only by running the search. So in your first pass, for every block:\n\n- **Run the search the design says it ran** - not a similar one. Claimed: nothing in `profiles/` grants the object → grep `profiles/` yourself and read what returns.\n- **Open the component whose property the design uses** - passing an attribute to an existing LWC claims that `@api` exists; read the file's `@api` list. Same for Apex signatures.\n- **Read the markup behind an ALREADY-PRESENT claim about a control** - \"Fiscal Year is a Single Select\" was a free-text `lightning-input`.\n- **Test the SCOPE of every assertion, yours and the design's** - true of one profile and false of the org is wrong in both directions.\n\nDepth costs nothing here: an unread file in round 1 is a round lost in round 3.\n\n## What to check, verifying against the ACTUAL codebase\n\n1. EVIDENCE is real - every component cited for ALREADY IMPLEMENTED/PARTIAL exists and does what is claimed. Open the file; never infer from the name. A wrong claim here is the worst failure mode.\n2. STATUS is honest - nothing marked implemented that is only similar.\n3. DESIGN covers the full PENDING scope, reuses existing components, and declarative-vs-code choices are justified.\n4. REJECTED and TRADE-OFF hold up: the alternative was a real option, the ruling constraint is true of THIS org, the trade-off bearable. A never-viable rejected option is padding; a '-' where an obvious alternative existed is a gap.\n5. COVERAGE, against the frozen requirement list below (never the source document - nothing re-reads it). Every REQ id has a block; within a block every AC is designed or accounted-for with evidence. Report a dropped AC as a finding refs'd to its REQ id. (46 of 56 ACs once had no owning design, unreported for four rounds.)\n6. Dependencies and sequencing make sense, both directions.\n7. Every carried `OPEN` is settled: `OPEN-RESOLVED` with a file and line that actually answers it, or `OPEN-CONFIRMED` with a reason the codebase truly cannot. Both directions are findings: a RESOLVED whose citation does not answer it, and a CONFIRMED this org plainly answers (a question to the customer about their own code). A live OPEN-CONFIRMED holds its block open by itself - if the design settled the question and only forgot to move the line, say so as a finding rather than leaving the block blocked on nothing.\n\n## What is NOT yours to review\n\nYou review a DESIGN. Test classes, code style, and build mechanics belong to the implementation workflow's own reviewer and validator - raising them here spends a round nobody can act on (\"no test classes are specified\" was raised four runs straight and was never the design's job). Document mechanics (heading levels, duplicate blocks, STATE lines, the ledger) belong to the tool: say it ONCE as a nit, never blocking.\n\nState the SCOPE of what you assert, not just an example - the design will be rebuilt around whichever you write.\n\nEvery finding names the requirement ids it concerns in `[refs: ...]` - that is how it files under the right block; unref'd findings land where that block's designer will not see them.\n\n## Say whether the design can actually close it\n\nEnd every finding with one of:\n\n    NEEDS: fix\n    NEEDS: decide - <the question, and who must answer it>\n\n`fix` = closable from what this org and requirement list already know. `decide` = the information does not exist yet (another team's schema, an open business rule, missing data) and no redesign will produce it. The loop stops when nothing `fix` remains - so a blocker mislabeled `fix` burns rounds designing around missing information, and a fixable defect mislabeled `decide` ships as a question. Your own Fix line gives it away: \"confirm with X\" or \"scope it out with an OPEN-CONFIRMED\" means `decide`.\n\nDesign under review:\n{steps.analyse.output}\n\nFROZEN REQUIREMENT LIST the design must cover:\n{steps.requirements.output}",
        "readOnly": true,
        "persona": "salesforce-architect",
        "role": "review",
        "autoRevise": {
          "target": "analyse",
          "trigger": "VERDICT:\\s*BLOCKED",
          "maxRounds": 3
        },
        "reviewOf": "analyse",
        "emits": "findings",
        "timeoutMinutes": 45
      },
      {
        "id": "approve-design",
        "title": "Review each requirement's design",
        "type": "gate",
        "message": "HUMAN GATE - you are shown: (1) the per-requirement design cards, (2) the design critique's unresolved findings (red panel, with each affected card flagged 'reviewer objects'). Decide: approve as-is, reject/comment individual cards, use 'Revise with ALL findings' to adopt the critique wholesale, or type your own instructions. Only after your approval are the HLD/TDD documents written.\n\nYour decision is recorded in the design document and outranks the critique: a card you approve is not reworked by the designer in later rounds, and a finding you overrule is not raised again as though you had not ruled on it. Where the reviewer objects to a card you already approved, you see the objection and the designer's answer to it side by side - the card changes only if you say so.",
        "reviseTarget": "analyse"
      },
      {
        "id": "work-check",
        "title": "Anything left to design for?",
        "type": "work-check"
      },
      {
        "id": "ux-design",
        "title": "UX design for the UI scope (project setting: UX enabled)",
        "type": "agent",
        "prompt": "UX DESIGN for the approved solution. DO NOT modify any files in this step.\nThe approved per-requirement design is at .dhruva/runs/{runId}/docs/design.md - read it completely. If your search tool reports no match, read the path directly: it is inside a dot-directory, which some search tools skip. Each block may carry its review history below a `<!-- lineage -->` marker; the design is the fields ABOVE it.\n\nPROJECT UX RULES (mandatory, from the project's settings):\n{inputs.uxRules}\n\n1. Read EVERY file in the project's standing design folder {inputs.designDir} in full (style guides, conventions). If it does not exist or is empty, say so - SLDS defaults apply.\n2. Identify the REQ blocks with USER-FACING scope (screens, components, record pages). Requirements with no UI scope are out of this step - list them as 'no UI'.\n3. For each UI-scoped requirement produce UX blocks, ids UX-1, UX-2..., each:\nUX-n (traces: REQ-xxx): <exact LWC API name>\n  PURPOSE: <what it does for the user>\n  COMPOSITION: <lightning base components / SLDS blueprints, layout hierarchy>\n  STATES: <loading, empty, error, success - what each shows>\n  INTERACTIONS: <events in/out, data contract with apex/wire>\n  ACCESSIBILITY: <labels, keyboard path, focus management, aria where needed>\n  REUSES: <existing components (verified in the codebase), or '-'>\nRules: SLDS only - no custom CSS unless the project UX rules or the design folder explicitly mandate it (cite the mandate). Reuse before new.",
        "readOnly": true,
        "role": "design",
        "artifact": ".dhruva/runs/{runId}/docs/ux-design.md",
        "onlyIf": "uxEnabled",
        "timeoutMinutes": 30
      },
      {
        "id": "ux-critique",
        "title": "UX critique (best model, read-only) - auto-fixes up to 3 times before your gate",
        "type": "agent",
        "prompt": "You are the UX REVIEWER - adversarially critique the UX design below before a human sees it. Do not modify any files.\n\nUX design under review:\n{steps.ux-design.output}\n\nApproved functional design it must serve. Each block may carry its review history below a `<!-- lineage -->` marker; the design is the fields ABOVE it:\n{steps.analyse.output}\n\nPROJECT UX RULES (the design must comply):\n{inputs.uxRules}\n\nCheck strictly: every UI-scoped REQ has UX blocks; no state is missing (loading/empty/error are the usual omissions); the project UX rules are followed; custom CSS only with a cited mandate; REUSES claims are real; accessibility is concrete, not 'accessible'.",
        "readOnly": true,
        "role": "review",
        "autoRevise": {
          "target": "ux-design",
          "trigger": "VERDICT:\\s*BLOCKED",
          "maxRounds": 3
        },
        "reviewOf": "ux-design",
        "emits": "findings",
        "onlyIf": "uxEnabled"
      },
      {
        "id": "approve-ux",
        "title": "Approve the UX design",
        "type": "gate",
        "message": "HUMAN GATE - you are shown the UX-n design blocks and the UX critique's unresolved findings. Revise with instructions to rework specific blocks, or approve - the documents then cover the UX design too.",
        "reviseTarget": "ux-design",
        "onlyIf": "uxEnabled"
      },
      {
        "id": "write-doc",
        "title": "Write HLD + TDD documents (with ERD)",
        "type": "agent",
        "prompt": "The approved design is already written, in full, at .dhruva/runs/{runId}/docs/design.md - one REQ block per requirement, each with STATUS, EVIDENCE, ALREADY-PRESENT, PENDING, DESIGN, EFFORT and DEPENDS-ON. Read it completely. If your search tool reports no match, read the path directly: it is inside a dot-directory, which some search tools skip, and a failed search is not evidence the file is absent.\n\nEach block may also carry its own review history, below a `<!-- lineage -->` marker: the findings raised against it and the designer's answers. That is the record of HOW the design reached its current form - the design itself is the fields ABOVE the marker. Write the documents from the fields. Read the history only when you need to know why something is the way it is, and never carry a superseded design into the documents because it appears in a finding.\n\nIf that file does not exist or has no REQ blocks, STOP and say so plainly - do not invent a design. It is the only copy; writing documents from memory would silently replace an approved design with a guess.\n\nYour job is NOT to restate it. Re-typing a gap analysis that already exists is how requirements get lost: on a real run five of them vanished between the design and the documents. Reference the design for anything it already says, and spend your effort on what it does NOT say.\n\nThe design is organised per REQUIREMENT. These documents are organised per CONCERN - data flow, error handling, governor limits, deployment order. That thinking cuts across all the REQ blocks at once and exists nowhere yet. That is the work.\n\nCreate or modify ONLY these three files:\n\n1. .dhruva/runs/{runId}/docs/hld.md - for stakeholders and review boards:\n  1. Context - the problem space and system boundaries this design stands on.\n  2. Goals / Non-Goals - what it achieves, and what it explicitly does not.\n  3. Gap Analysis - DO NOT re-tabulate the REQ blocks. One short paragraph of the shape of the gap (how many already implemented / partial / new, and the theme), then: \"Per-requirement detail: see design.md.\"\n  4. Approaches Considered - assemble this from the REJECTED lines the design already recorded: per decision, the option taken, the option rejected, and the constraint that ruled it out. Do NOT invent alternatives the design does not name - a deliberation nobody had, written up after approval, is the one section of this document no reviewer ever checked. Add the overall complexity (XS/S/M/L/XL) for the selected approach.\n  5. High-Level Design - how the component groups interact, and the key abstractions introduced. Architecture, not a per-REQ list.\n  6. Data Model - a Mermaid er-diagram (```mermaid / erDiagram) of new and impacted objects, their relationships and key fields.\n  7. Integration Touchpoints and Security Model.\n  8. Constraints and Trade-offs - assemble from the design's TRADE-OFF lines; add only constraints that emerge across requirements rather than within one.\n  9. Acceptance Criteria - \"AC-n: Given <precondition>, when <action>, then <outcome>. [traces: REQ-xxx]\". Every REQ with pending work needs at least one AC.\n  10. Impact analysis, risks and assumptions, Open Questions. For effort, total the design's EFFORT values and show the total plus any contingent items - do not re-list every requirement.\n\n2. .dhruva/runs/{runId}/docs/tdd.md - for the developers who build it:\n  1. Components - per apex class, trigger, LWC, flow, object and field: exact API names, responsibility, inputs/outputs, dependencies, reuse-vs-new, and method-level design or pseudocode where non-trivial.\n  2. Data Flow - how data moves end to end.\n  3. State Management - what state exists, where it lives, how it changes.\n  4. Error Handling - what can fail, and how each failure is handled and surfaced.\n  5. Governor Limits and Sharing/FLS specifics.\n  6. Deployment Order and dependencies.\n  7. Test Strategy - the test classes and scenarios (positive, negative, bulk). Apex tests run in the org, so tests ship WITH the implementation and are proven by a check-only deploy with RunLocalTests; never promise a failing-tests-first cycle.\n  8. Build Plan - a short summary table of the tasks file (T-n | title | depends on | traces). The tasks file is the authority.\n  9. Decisions - one line each: Decision -> Rationale -> Consequence.\n  10. Open Questions that may affect implementation.\n\n3. .dhruva/runs/{runId}/docs/tasks.json - the machine-readable build plan the implementation workflow executes. Strict JSON, no comments, no trailing commas, exactly this shape:\n{ \"version\": 1, \"tasks\": [ { \"id\": \"T-1\", \"title\": \"<imperative, one line>\", \"depends_on\": [], \"files\": [\"force-app/main/default/classes/Example.cls\"], \"change\": \"<the mechanism - what edit, where>\", \"test_scenarios\": [\"<case>\"], \"traces\": [\"REQ-001\", \"AC-1\"], \"status\": \"pending\" } ] }\nRules: ids sequential from T-1; every task lists the project-relative files it touches and traces to at least one REQ or AC; depends_on names only earlier tasks; no cycles; one component plus its test class per task; order is safe deployment order. Take file paths from the design's EVIDENCE where it names them - guess a path only when the design gives none, and follow this project's existing folder layout when you do.\n\nDo not contradict the approved design. Where you need precision it does not give, add it; where it already decided something, cite it rather than re-deciding.\n\nThe Mermaid block must be valid erDiagram syntax - every relationship line has both entities and a label - with no styling directives.\n\nAPPROVED UX DESIGN: if .dhruva/runs/{runId}/docs/ux-design.md exists, read it completely and incorporate it fully (when UX is disabled for this project the file is absent - then skip this paragraph). The TDD gains a 'UI Components' section carrying every UX-n block, and the tasks file gets one task per UX component (files under force-app/main/default/lwc/..., traces include the UX-n id AND its REQ id).",
        "persona": "salesforce-writer",
        "role": "design",
        "timeoutMinutes": 30
      },
      {
        "id": "changes",
        "title": "Collect created documents",
        "type": "changes"
      },
      {
        "id": "tasks-check",
        "title": "Validate the build-plan tasks file (deterministic)",
        "type": "tasks-check",
        "tasksFile": ".dhruva/runs/{runId}/docs/tasks.json"
      },
      {
        "id": "coverage-check",
        "title": "Verify the documents cover every requirement (agent, read-only)",
        "type": "agent",
        "prompt": "Design coverage verification. Do not modify any files.\nRead IN FULL: (1) .dhruva/runs/{runId}/docs/hld.md, (2) .dhruva/runs/{runId}/docs/tdd.md. If your search tool reports no match, read the paths directly - they are inside a dot-directory, which some search tools skip.\n\nFROZEN REQUIREMENT LIST (the agreed requirement set - check against this, not the source document):\n{steps.requirements.output}\n\nApproved per-requirement design. Each block may carry its review history below a `<!-- lineage -->` marker; the design is the fields ABOVE it, and a superseded approach quoted inside a finding is not the design:\n{steps.analyse.output}\n\nFor EVERY REQ item, report one line:\n  REQ-xxx - COVERED (HLD section N / TDD section M) | MISSING FROM DOCS | DIVERGES (the docs say something different from the approved design - quote it)\nAlso flag anything in the docs that has no approved requirement behind it (scope creep), any REQ with pending work that no Acceptance Criterion traces ([traces: REQ-xxx]), and any Build Plan task in the TDD that traces to nothing.\nIf an approved UX design exists (below), ALSO report one line per UX-n: covered in the TDD's UI section + a task traces to it, or MISSING.\nApproved UX design (may be empty):\n{steps.ux-design.output}",
        "readOnly": true,
        "role": "trace",
        "autoRevise": {
          "target": "write-doc",
          "trigger": "COVERAGE:\\s*INCOMPLETE",
          "maxRounds": 1
        },
        "emits": "coverage",
        "timeoutMinutes": 30
      },
      {
        "id": "approve-doc",
        "title": "Accept the design documents",
        "type": "gate",
        "message": "HUMAN GATE - you are shown the coverage verdict and any unresolved findings; the documents are in .dhruva/runs/{runId}/docs/. If items are MISSING or DIVERGE, revise; accept to complete the run.",
        "reviseTarget": "write-doc"
      }
    ]
  },
  "test-gen": {
    "id": "test-gen",
    "title": "Test generation",
    "description": "Add meaningful Apex tests to existing code: assess coverage gaps for the target classes, gate on the test plan, write the tests, validate with local tests, deploy to the sandbox.",
    "inputs": [
      {
        "key": "target",
        "label": "Target classes (comma-separated names, or describe the area)",
        "kind": "text"
      },
      {
        "key": "deploy",
        "label": "Deploy the new tests to the connected sandbox",
        "kind": "boolean",
        "default": true
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot baseline",
        "type": "snapshot"
      },
      {
        "id": "assess",
        "title": "Assess coverage gaps (agent, read-only)",
        "type": "agent",
        "prompt": "Target: {inputs.target}\nRead the target classes IN FULL and their existing test classes (if any). DO NOT modify any files in this step. Identify the untested or weakly tested behavior: public methods, branches, error paths, bulk behavior, permission-sensitive logic. Propose a test plan: which test classes to create or extend, and the scenarios per class (positive / negative / bulk / permission). Note whether a shared TestDataFactory exists and what it is missing.\nEnd with one line listing every EXISTING file you will modify as project-relative paths:\nFILES: force-app/main/default/classes/ExampleTest.cls",
        "readOnly": true,
        "persona": "salesforce-test",
        "role": "read"
      },
      {
        "id": "approve-plan",
        "title": "Approve the test plan",
        "type": "gate",
        "message": "Review the coverage assessment and test plan (revise with instructions to change scope or scenarios). Proceed with writing the tests?"
      },
      {
        "id": "implement",
        "title": "Write the tests (agent)",
        "type": "agent",
        "prompt": "Write the tests per the approved plan:\n{steps.assess.output}\n\nRules of engagement: modify/create TEST classes (and the shared TestDataFactory if needed) ONLY - never change the production classes under test in this workflow; if a class is untestable as written, report it instead of changing it. Follow the apex-tests standards: TestDataFactory data, Assert class with messages, positive/negative/bulk, System.runAs with permission sets, no SeeAllData. Meaningful assertions over coverage percentage.",
        "role": "implement"
      },
      {
        "id": "changes",
        "title": "Collect changed files",
        "type": "changes"
      },
      {
        "id": "verify-standards",
        "title": "Verify standards on changed files (deterministic)",
        "type": "verify"
      },
      {
        "id": "approve-changes",
        "title": "Approve the new tests",
        "type": "gate",
        "message": "Review the test diffs (links above); type instructions and Revise to rework them. Validate them against the org?",
        "reviseTarget": "implement"
      },
      {
        "id": "validate",
        "title": "Validate with local tests (proves the new tests pass)",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "{changedSourceDirs}",
          "--test-level",
          "RunLocalTests",
          "--json",
          "--wait",
          "60"
        ]
      },
      {
        "id": "approve-deploy",
        "title": "Approve deploy to the connected sandbox",
        "type": "gate",
        "message": "Tests validated. Deploy them to the connected sandbox?",
        "onlyIf": "deploy"
      },
      {
        "id": "deploy",
        "title": "Deploy the tests",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "{changedSourceDirs}",
          "--json",
          "--wait",
          "30"
        ],
        "onlyIf": "deploy"
      }
    ]
  },
  "ux-design": {
    "id": "ux-design",
    "title": "UX design",
    "description": "LWC-heavy path: read the project's standing design folder, design each screen/component (SLDS-only, states, accessibility), gate, then write a developer-ready UX spec + build-plan tasks. Implement it afterwards with 'Implement from TDD' pointed at the UX spec.",
    "inputs": [
      {
        "key": "requirement",
        "label": "Feature / UX requirement (paste text and/or attach documents below)",
        "kind": "text",
        "attachTo": true
      },
      {
        "key": "designDir",
        "label": "Standing design-inputs folder in the project (style guides, screenshots, conventions)",
        "kind": "text",
        "default": "docs/design"
      },
      {
        "key": "docName",
        "label": "UX document name (file-safe)",
        "kind": "text",
        "default": "ux-design"
      }
    ],
    "steps": [
      {
        "id": "snapshot",
        "title": "Snapshot baseline",
        "type": "snapshot"
      },
      {
        "id": "design-context",
        "title": "Read the design folder + existing components (read-only)",
        "type": "agent",
        "prompt": "Binary attachments (.docx/.pdf) cannot be read directly - when one is referenced, read the extracted sibling file <same-name>.extracted.md instead (created at upload); only report a document unreadable if no extracted sibling exists.\nCONTEXT GATHERING ONLY - do not design anything and do not modify any files.\nRequirement:\n{inputs.requirement}\n\n1. Read EVERY file in the project's standing design folder {inputs.designDir} in full (style guides, brand rules, screenshots described by their file names, component conventions). If the folder does not exist or is empty, say so explicitly and note that SLDS defaults apply.\n2. If the requirement references attached documents (paths under .dhruva/attachments/), read them in full too.\n3. Inventory the existing LWCs under force-app that are relevant to this requirement: component name, what it renders today, reusable parts (verified by reading the markup/JS, never guessed from the name).\nOutput: DESIGN CONVENTIONS (what the design folder mandates), REUSABLE COMPONENTS (what exists), and GAPS (what has no precedent).",
        "readOnly": true,
        "role": "read",
        "timeoutMinutes": 20
      },
      {
        "id": "ux-design",
        "title": "Design each screen/component (SLDS-only)",
        "type": "agent",
        "prompt": "Design the UX for this requirement. DO NOT modify any files in this step.\nRequirement:\n{inputs.requirement}\n\nDesign context from the previous step (conventions, reusable components, gaps):\n{steps.design-context.output}\n\nRules: SLDS ONLY - lightning base components and SLDS utility classes; NO custom CSS unless the design folder explicitly mandates it (then cite the mandate). Reuse the inventoried components before proposing new ones.\nOutput ONE BLOCK PER SCREEN/COMPONENT, ids UX-1, UX-2...:\nUX-n: <component name (exact LWC API name)>\n  PURPOSE: <what it does for the user>\n  COMPOSITION: <lightning base components / SLDS blueprints used, layout hierarchy>\n  STATES: <loading, empty, error, success - what each shows>\n  INTERACTIONS: <events in/out, data contract with apex/wire>\n  ACCESSIBILITY: <labels, keyboard path, focus management, aria where needed>\n  REUSES: <existing components used, or '-'>\nEnd with OPEN QUESTIONS (or 'none').",
        "readOnly": true,
        "role": "design",
        "timeoutMinutes": 30
      },
      {
        "id": "ux-critique",
        "title": "UX critique (best model, read-only) - auto-fixes up to 3 times before your gate",
        "type": "agent",
        "prompt": "You are the UX REVIEWER - adversarially critique the UX design below before a human sees it. Do not modify any files.\n\nDesign under review:\n{steps.ux-design.output}\n\nRequirement:\n{inputs.requirement}\n\nCheck strictly:\n1. Every screen/flow the requirement implies has a UX-n block; no state is missing (loading/empty/error are the usual omissions).\n2. SLDS-only holds - flag any custom CSS without a cited design-folder mandate.\n3. REUSES claims are real (the cited components exist and fit).\n4. Accessibility is concrete (actual labels/keyboard paths, not 'accessible').",
        "readOnly": true,
        "role": "review",
        "autoRevise": {
          "target": "ux-design",
          "trigger": "VERDICT:\\s*BLOCKED",
          "maxRounds": 3
        },
        "emits": "findings"
      },
      {
        "id": "approve-ux",
        "title": "Approve the UX design",
        "type": "gate",
        "message": "Review each UX-n block and the critique above. Revise with instructions to rework specific blocks, or approve - the UX spec document and build-plan tasks are then written.",
        "reviseTarget": "ux-design"
      },
      {
        "id": "write-doc",
        "title": "Write the UX spec document + build-plan tasks",
        "type": "agent",
        "prompt": "Write the APPROVED UX design as a developer-ready spec plus a machine-readable tasks file (create folders if needed). These two files are the only ones you may create or modify in this step:\n\n1. .dhruva/runs/{runId}/docs/ux.md - the UX SPEC, numbered sections: 1. Context and design conventions applied; 2. Component inventory (per UX-n: exact LWC API name, composition, states, interactions/data contract, accessibility, reuse decisions); 3. Navigation/flow between components; 4. Test strategy (Jest per component: states, events, accessibility asserts); 5. Open questions. Plain ASCII punctuation, exact API names in backticks, tables where they beat prose.\n\n2. .dhruva/runs/{runId}/docs/tasks.json - strict JSON, exactly this shape:\n{ \"version\": 1, \"tasks\": [ { \"id\": \"T-1\", \"title\": \"<imperative>\", \"depends_on\": [], \"files\": [\"force-app/main/default/lwc/example/example.html\"], \"change\": \"<what to build>\", \"test_scenarios\": [\"<case>\"], \"traces\": [\"UX-1\"], \"status\": \"pending\" } ] }\nOne component (markup+JS+meta plus its Jest test) per task; every task traces to at least one UX-n; depends_on only references earlier tasks; order = safe build order.\n\nApproved UX design:\n{steps.ux-design.output}\n\nIncorporate every approved detail; do not contradict what was approved.",
        "persona": "salesforce-writer",
        "role": "design",
        "timeoutMinutes": 30
      },
      {
        "id": "changes",
        "title": "Collect created documents",
        "type": "changes"
      },
      {
        "id": "tasks-check",
        "title": "Validate the build-plan tasks file (deterministic)",
        "type": "tasks-check",
        "tasksFile": ".dhruva/runs/{runId}/docs/tasks.json"
      },
      {
        "id": "coverage-check",
        "title": "Verify the spec covers every UX block (read-only)",
        "type": "agent",
        "prompt": "Coverage verification. Do not modify any files.\nRead .dhruva/runs/{runId}/docs/ux.md and .dhruva/runs/{runId}/docs/tasks.json in full.\nApproved UX design:\n{steps.ux-design.output}\n\nFor EVERY UX-n block report one line:\n  UX-n - COVERED (spec section + task id) | MISSING FROM SPEC | NO TASK | DIVERGES (quote it)",
        "readOnly": true,
        "role": "trace",
        "autoRevise": {
          "target": "write-doc",
          "trigger": "COVERAGE:\\s*INCOMPLETE",
          "maxRounds": 1
        },
        "emits": "coverage",
        "timeoutMinutes": 20
      },
      {
        "id": "approve-doc",
        "title": "Accept the UX spec",
        "type": "gate",
        "message": "Review the coverage verdict and the spec (.dhruva/runs/{runId}/docs/). Accept to complete - then build it with 'Implement from TDD': TDD path = .dhruva/runs/{runId}/docs/ux.md, tasks file = .dhruva/runs/{runId}/docs/tasks.json, visual test ON.",
        "reviseTarget": "write-doc"
      }
    ]
  },
  "validate-deploy": {
    "id": "validate-deploy",
    "title": "Validate deploy",
    "description": "Gate, then check-only deploy of a source path with the chosen test level. Works on any org (no source tracking required) - nothing is ever saved to the org.",
    "inputs": [
      {
        "key": "target",
        "label": "Source path to validate",
        "kind": "text",
        "default": "force-app"
      },
      {
        "key": "testLevel",
        "label": "Test level",
        "kind": "select",
        "options": [
          "NoTestRun",
          "RunLocalTests",
          "RunAllTestsInOrg"
        ],
        "default": "RunLocalTests"
      }
    ],
    "steps": [
      {
        "id": "approve",
        "title": "Approve validation",
        "type": "gate",
        "message": "Run a check-only deploy of {inputs.target} against the default org (compiles + runs the chosen tests; nothing is saved)?"
      },
      {
        "id": "validate",
        "title": "Check-only deploy",
        "type": "cli",
        "bin": "sf",
        "args": [
          "project",
          "deploy",
          "start",
          "--dry-run",
          "--source-dir",
          "{inputs.target}",
          "--test-level",
          "{inputs.testLevel}",
          "--json",
          "--wait",
          "60"
        ]
      }
    ]
  }
}
